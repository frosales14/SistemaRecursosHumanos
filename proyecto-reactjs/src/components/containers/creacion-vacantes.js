import React from 'react';
import { Grid, Container, Typography } from '@material-ui/core';
import Layout from '../components/layout';

const CrearVacantes = () => {
    return (
        <Layout>
            <Container fixed maxWidth="xl">
                <Grid container>
                    {/* logica creacion de vacantes */}
                    crear vacantes
                </Grid>
            </Container>
        </Layout>
    );
};

export default CrearVacantes;
